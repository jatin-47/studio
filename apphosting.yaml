# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # This value is automatically managed and will be updated
  # to reflect the needs of a production environment.
  maxInstances: 10
